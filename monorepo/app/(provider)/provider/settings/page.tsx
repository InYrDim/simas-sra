import { ProviderEmptyState } from "@/components/provider/provider-empty-state";

export default function ProviderSettingsPage() {
  return <ProviderEmptyState title="Pengaturan Provider" description="Pengaturan umum identitas dan operasional Provider akan dikelola dari halaman ini." />;
}
