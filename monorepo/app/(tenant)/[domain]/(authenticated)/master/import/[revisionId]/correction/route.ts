import { buildCorrectionWorkbook } from "@/lib/imports/people-import-review";
import { getImportReview } from "@/lib/imports/people-import-review-data";
import { enforceTenantFeatureAccess } from "@/lib/features/tenant-feature-route-access";
export async function GET(_request:Request,{params}:{params:Promise<{domain:string;revisionId:string}>}){const{domain,revisionId}=await params,principal=await enforceTenantFeatureAccess(domain,"masterDataImportDownload","download"),review=await getImportReview(principal,revisionId);if(!review)return new Response(null,{status:404});const bytes=await buildCorrectionWorkbook(review.revision.kind,review.rows);return new Response(bytes as BodyInit,{headers:{"Content-Type":"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet","Content-Disposition":`attachment; filename="koreksi-${review.revision.kind}-${revisionId}.xlsx"`,"Cache-Control":"no-store"}});}
