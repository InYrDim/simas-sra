import assert from "node:assert/strict";import { readFile } from "node:fs/promises";import test from "node:test";
test("Organisasi Siswa uses shared responsive history UX and explicit no-cascade actions",async()=>{const source=await readFile(new URL("../app/(tenant)/[domain]/(authenticated)/master/organisasi/page.tsx",import.meta.url),"utf8");for(const text of ["MasterDataWorkspace","Periode Kepengurusan","Keanggotaan Organisasi","Nama jabatan","Boleh beberapa pemegang","Koreksi khusus","Blocker archive","tidak mengakhiri periode, membership, leadership, atau lokasi","sm:grid-cols-2"])assert.match(source,new RegExp(text));});
