import assert from "node:assert/strict";import {readFile} from "node:fs/promises";import test from "node:test";test("Ekstrakurikuler uses shared responsive list/detail and explicit history UX",async()=>{const source=await readFile(new URL("../app/(tenant)/[domain]/(authenticated)/master/organisasi/ekstrakurikuler/page.tsx",import.meta.url),"utf8");for(const text of ["MasterDataWorkspace","Kelompok Kegiatan","Riwayat pembina dan peserta","Kapasitas","Blocker archive","tidak mengakhiri kelompok, pembina, peserta, atau referensi lokasi","sm:grid-cols-2"])assert.match(source,new RegExp(text))});