import assert from "node:assert/strict";import { randomUUID } from "node:crypto";import { readFile } from "node:fs/promises";import test,{after} from "node:test";import mysql from "mysql2/promise";import { closeDatabasePool } from "@/db";import { createLocationService } from "@/lib/location";import { locationStore } from "@/lib/location-data";import type { MasterDataPrincipal } from "@/lib/tenant-master-data-access";
const databaseUrl=process.env.DATABASE_URL,mysqlTest=databaseUrl?test:test.skip;after(()=>closeDatabasePool());
mysqlTest("MySQL serializes concurrent hierarchy edits and preserves Tenant isolation",async()=>{const connection=await mysql.createConnection(databaseUrl!),migration=await readFile(new URL("../drizzle/20260720121500_add-location-hierarchy/migration.sql",import.meta.url),"utf8");for(const statement of migration.replaceAll("--> statement-breakpoint", "").split(";").map(x=>x.trim()).filter(Boolean)){try{await connection.query(statement)}catch(error){if(![1050,1060,1061,1826].includes((error as {errno?:number}).errno??0))throw error;}}const ids={tenant:randomUUID(),owner:randomUUID(),provider:randomUUID(),binding:randomUUID(),application:randomUUID()},npsn=String(Math.floor(10_000_000+Math.random()*90_000_000));try{await connection.execute("INSERT INTO `user` (`id`,`name`,`email`,`email_verified`,`created_at`,`updated_at`) VALUES (?, 'Admin', ?, false, NOW(3), NOW(3)), (?, 'Provider', ?, false, NOW(3), NOW(3))",[ids.owner,`${ids.owner}@test.invalid`,ids.provider,`${ids.provider}@test.invalid`]);await connection.execute("INSERT INTO `provider_admin` (`user_id`,`created_at`) VALUES (?,NOW(3))",[ids.provider]);await connection.execute("INSERT INTO `applicant_school_binding` (`id`,`user_id`,`canonical_npsn`,`created_at`) VALUES (?,?,?,NOW(3))",[ids.binding,ids.owner,npsn]);await connection.execute("INSERT INTO `simas_application` (`id`,`school_name`,`npsn`,`education_level`,`address`,`contact_name`,`contact_position`,`contact_email`,`contact_whatsapp`,`status`,`submitted_at`,`owner_user_id`,`binding_id`,`attempt_number`,`idempotency_key`,`payload_hash`) VALUES (?, 'Sekolah Uji', ?, 'SMA', 'Alamat', 'Kontak', 'Operator', ?, '0812', 'pending', NOW(3), ?, ?, 1, ?, REPEAT('a',64))",[ids.application,npsn,`${ids.application}@test.invalid`,ids.owner,ids.binding,randomUUID()]);await connection.execute("INSERT INTO `tenant` (`id`,`name`,`domain`,`npsn`,`source_application_id`,`approved_at`,`operational_status`,`created_at`,`updated_at`) VALUES (?, 'Sekolah Uji', ?, ?, ?, NOW(3), 'active', NOW(3), NOW(3))",[ids.tenant,`location-${randomUUID()}`,npsn,ids.application]);await connection.execute("UPDATE `simas_application` SET `status`='approved',`decided_at`=NOW(3),`decided_by_provider_admin_id`=?,`approved_tenant_id`=? WHERE id=?",[ids.provider,ids.tenant,ids.application]);await connection.execute("UPDATE `user` SET tenant_id=?,tenant_role='school-admin' WHERE id=?",[ids.tenant,ids.owner]);const principal:MasterDataPrincipal={userId:ids.owner,tenantId:ids.tenant,role:"school-admin",capabilities:{read:true,write:true,downloadTemplate:true}},service=createLocationService({store:locationStore}),a=await service.create(principal,{name:"A",code:"A",type:"building"}),b=await service.create(principal,{name:"B",code:"B",type:"building"});assert.equal(a.ok,true);assert.equal(b.ok,true);if(!a.ok||!b.ok)return;const results=await Promise.all([service.edit(principal,a.record.id,{name:"A",code:"A",type:"building",parentId:b.record.id},1),service.edit(principal,b.record.id,{name:"B",code:"B",type:"building",parentId:a.record.id},1)]);assert.equal(results.filter(x=>x.ok).length,1);assert.equal(results.filter(x=>!x.ok&&x.code==="hierarchy-cycle").length,1);assert.deepEqual(await service.list({...principal,tenantId:randomUUID()}),[]);}finally{await connection.execute("SET FOREIGN_KEY_CHECKS=0");for(const table of ["location_reference","location_history","location"])await connection.execute(`DELETE FROM \`${table}\` WHERE tenant_id=?`,[ids.tenant]);await connection.execute("UPDATE `user` SET tenant_id=NULL,tenant_role=NULL WHERE id=?",[ids.owner]);await connection.execute("DELETE FROM `tenant` WHERE id=?",[ids.tenant]);await connection.execute("DELETE FROM `simas_application` WHERE id=?",[ids.application]);await connection.execute("DELETE FROM `applicant_school_binding` WHERE id=?",[ids.binding]);await connection.execute("DELETE FROM `provider_admin` WHERE user_id=?",[ids.provider]);await connection.execute("DELETE FROM `user` WHERE id IN (?,?)",[ids.owner,ids.provider]);await connection.execute("SET FOREIGN_KEY_CHECKS=1");await connection.end();}});
